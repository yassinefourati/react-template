commit_msg=$(cat "$1")
pattern="^(feat|fix|chore|docs|style|refactor|test|perf|build|ci)(\(.+\))?: .{1,100}$"
if ! echo "$commit_msg" | grep -qE "$pattern"; then
  echo "Commit must follow Conventional Commits: feat|fix|chore(scope): description"
  exit 1
fi
